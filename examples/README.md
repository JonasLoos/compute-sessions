# Example configs

Known-good `~/.config/compute-sessions/config.toml` contents for specific clusters. Copy one and adjust the ssh alias.

- [config.toml](config.toml) — fully annotated example covering every option.
- [hydra.toml](hydra.toml) — the TU Berlin ML-group hydra cluster.

If you set up a cluster others might use too, consider PRing its config here.
